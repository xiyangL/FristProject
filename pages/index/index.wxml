<!--pages/wxml/index.wxml-->
<view>25.66666</view>

<!-- <wxs module="info">
  // JS代码
  var message = "Hello World";
  var name = "coderwhy"; 

  function sum(num1, num2) {
    return num1 + num2
  }

  function test() {
    return "test function"
  }

  // commonjs的模块化导出的写法
  module.exports = {
    message: message,
    name: name,
    sum: sum,
    test: test
  }
</wxs> -->

<wxs src="../../wxs/info.wxs" module="info"/>
<view>{{info.sum(10,20)}}</view>

<wxs src="../../wxs/format.wxs" module="format"/>

<view>{{format.priceFormat(2.2345,3)}}</view>

<view>{{format.dateFormat(1585252446, "yyyy-MM-dd hh:mm:ss")}}</view>

